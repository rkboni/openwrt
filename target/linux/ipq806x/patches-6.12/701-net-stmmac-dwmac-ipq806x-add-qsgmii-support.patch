--- a/drivers/net/ethernet/stmicro/stmmac/stmmac_platform.c
+++ b/drivers/net/ethernet/stmicro/stmmac/stmmac_platform.c
@@ -420,6 +420,7 @@ stmmac_probe_config_dt(struct platform_d
 	struct plat_stmmacenet_data *plat;
 	struct stmmac_dma_cfg *dma_cfg;
 	static int bus_id = -ENODEV;
+	const char *label;
 	int phy_mode;
 	void *ret;
 	int rc;
@@ -467,6 +468,11 @@ stmmac_probe_config_dt(struct platform_d
 	/* Default to phy auto-detection */
 	plat->phy_addr = -1;
 
+	/* Get custom label for network device name */
+	if (!of_property_read_string(np, "label", &label)) {
+		plat->label = devm_kstrdup(&pdev->dev, label, GFP_KERNEL);
+	}
+
 	/* Default to get clk_csr from stmmac_clk_csr_set(),
 	 * or get clk_csr from device tree.
 	 */
@@ -729,7 +735,6 @@ int stmmac_get_platform_resources(struct
 	if (stmmac_res->wol_irq < 0) {
 		if (stmmac_res->wol_irq == -EPROBE_DEFER)
 			return -EPROBE_DEFER;
-		dev_info(&pdev->dev, "IRQ eth_wake_irq not found\n");
 		stmmac_res->wol_irq = stmmac_res->irq;
 	}
 
@@ -738,7 +743,6 @@ int stmmac_get_platform_resources(struct
 	if (stmmac_res->lpi_irq < 0) {
 		if (stmmac_res->lpi_irq == -EPROBE_DEFER)
 			return -EPROBE_DEFER;
-		dev_info(&pdev->dev, "IRQ eth_lpi not found\n");
 	}
 
 	stmmac_res->sfty_irq =
@@ -746,7 +750,6 @@ int stmmac_get_platform_resources(struct
 	if (stmmac_res->sfty_irq < 0) {
 		if (stmmac_res->sfty_irq == -EPROBE_DEFER)
 			return -EPROBE_DEFER;
-		dev_info(&pdev->dev, "IRQ sfty not found\n");
 	}
 
 	stmmac_res->addr = devm_platform_ioremap_resource(pdev, 0);
--- a/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
+++ b/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
@@ -7806,6 +7806,10 @@ int stmmac_dvr_probe(struct device *devi
 		goto error_phy_setup;
 	}
 
+	if (plat_dat->label) {
+		strscpy(ndev->name, plat_dat->label, IFNAMSIZ);
+	}
+
 	ret = register_netdev(ndev);
 	if (ret) {
 		dev_err(priv->device, "%s: ERROR %i registering the device\n",
--- a/include/linux/stmmac.h
+++ b/include/linux/stmmac.h
@@ -220,6 +220,7 @@ struct plat_stmmacenet_data {
 	int maxmtu;
 	int multicast_filter_bins;
 	int unicast_filter_entries;
+	const char *label;
 	int tx_fifo_size;
 	int rx_fifo_size;
 	u32 host_dma_width;
--- a/drivers/net/ethernet/stmicro/stmmac/dwmac-ipq806x.c
+++ b/drivers/net/ethernet/stmicro/stmmac/dwmac-ipq806x.c
@@ -29,43 +29,49 @@
 #include <linux/module.h>
 #include <linux/sys_soc.h>
 #include <linux/bitfield.h>
+#include <linux/mdio.h>
+#include <linux/stdarg.h>
+#include <linux/netdevice.h>
+#include <linux/rtnetlink.h>
 
 #include "stmmac_platform.h"
 
-#define NSS_COMMON_CLK_GATE			0x8
+#define NSS_COMMON_CLK_GATE					0x8
 #define NSS_COMMON_CLK_GATE_PTP_EN(x)		BIT(0x10 + x)
 #define NSS_COMMON_CLK_GATE_RGMII_RX_EN(x)	BIT(0x9 + (x * 2))
 #define NSS_COMMON_CLK_GATE_RGMII_TX_EN(x)	BIT(0x8 + (x * 2))
 #define NSS_COMMON_CLK_GATE_GMII_RX_EN(x)	BIT(0x4 + x)
 #define NSS_COMMON_CLK_GATE_GMII_TX_EN(x)	BIT(0x0 + x)
 
-#define NSS_COMMON_CLK_DIV0			0xC
-#define NSS_COMMON_CLK_DIV_OFFSET(x)		(x * 8)
+#define NSS_COMMON_CLK_DIV0				0xC
+#define NSS_COMMON_CLK_DIV_OFFSET(x)	(x * 8)
 #define NSS_COMMON_CLK_DIV_MASK			0x7f
 
-#define NSS_COMMON_CLK_SRC_CTRL			0x14
+#define NSS_COMMON_CLK_SRC_CTRL				0x14
 #define NSS_COMMON_CLK_SRC_CTRL_OFFSET(x)	(x)
 /* Mode is coded on 1 bit but is different depending on the MAC ID:
  * MAC0: QSGMII=0 RGMII=1
  * MAC1: QSGMII=0 SGMII=0 RGMII=1
  * MAC2 & MAC3: QSGMII=0 SGMII=1
  */
-#define NSS_COMMON_CLK_SRC_CTRL_RGMII(x)	1
-#define NSS_COMMON_CLK_SRC_CTRL_SGMII(x)	((x >= 2) ? 1 : 0)
+#define NSS_COMMON_CLK_SRC_CTRL_RGMII(x)	(1)
+#define NSS_COMMON_CLK_SRC_CTRL_SGMII(x)	((x) >= 2 ? 1 : 0)
+#define NSS_COMMON_CLK_SRC_CTRL_QSGMII(x)	(0)
 
-#define NSS_COMMON_GMAC_CTL(x)			(0x30 + (x * 4))
+#define NSS_COMMON_GMAC_CTL(x)				(0x30 + (x * 4))
 #define NSS_COMMON_GMAC_CTL_CSYS_REQ		BIT(19)
 #define NSS_COMMON_GMAC_CTL_PHY_IFACE_SEL	BIT(16)
 #define NSS_COMMON_GMAC_CTL_IFG_LIMIT_OFFSET	8
 #define NSS_COMMON_GMAC_CTL_IFG_OFFSET		0
 
-#define NSS_COMMON_CLK_DIV_RGMII_1000		1
-#define NSS_COMMON_CLK_DIV_RGMII_100		9
+#define NSS_COMMON_CLK_DIV_RGMII_1000	1
+#define NSS_COMMON_CLK_DIV_RGMII_100	9
 #define NSS_COMMON_CLK_DIV_RGMII_10		99
-#define NSS_COMMON_CLK_DIV_SGMII_1000		0
-#define NSS_COMMON_CLK_DIV_SGMII_100		4
+#define NSS_COMMON_CLK_DIV_SGMII_1000	0
+#define NSS_COMMON_CLK_DIV_SGMII_100	4
 #define NSS_COMMON_CLK_DIV_SGMII_10		49
 
+#define QSGMII_PCS_ALL_CH_CTL_CH_EN(x)	BIT(28 + (x))
 #define QSGMII_PCS_ALL_CH_CTL			0x80
 #define QSGMII_PCS_CH_SPEED_FORCE		BIT(1)
 #define QSGMII_PCS_CH_SPEED_10			0x0
@@ -77,24 +83,38 @@
 						 QSGMII_PCS_CH_SPEED_1000)
 #define QSGMII_PCS_CH_SPEED_SHIFT(x)		((x) * 4)
 
-#define QSGMII_PCS_CAL_LCKDT_CTL		0x120
+#define QSGMII_PCS_CAL_LCKDT_CTL			0x120
 #define QSGMII_PCS_CAL_LCKDT_CTL_RST		BIT(19)
 
+#define QSGMII_PCS_MODE_CTL					0x68
+#define QSGMII_PCS_MODE_CTL_AUTONEG_EN(x)	BIT(x)
+#define QSGMII_PCS_SGMII_MAC				BIT(1)
+#define QSGMII_PCS_CAL_LCKDT_CTL_LCKDT		BIT(0)
+
+/* Per-channel QSGMII PCS channel IDs */
+#define QSGMII_PCS_CHAN0			0
+#define QSGMII_PCS_CHAN1			1
+#define QSGMII_PCS_CHAN2			2
+#define QSGMII_PCS_CHAN3			3
+
+/* NSS GMAC phy-profile values */
+#define NSS_GMAC_PHY_PROFILE_QSGMII		2
+
 /* Only GMAC1/2/3 support SGMII and their CTL register are not contiguous */
 #define QSGMII_PHY_SGMII_CTL(x)			((x == 1) ? 0x134 : \
 						 (0x13c + (4 * (x - 2))))
-#define QSGMII_PHY_CDR_EN			BIT(0)
+#define QSGMII_PHY_CDR_EN				BIT(0)
 #define QSGMII_PHY_RX_FRONT_EN			BIT(1)
-#define QSGMII_PHY_RX_SIGNAL_DETECT_EN		BIT(2)
+#define QSGMII_PHY_RX_SIGNAL_DETECT_EN	BIT(2)
 #define QSGMII_PHY_TX_DRIVER_EN			BIT(3)
 #define QSGMII_PHY_QSGMII_EN			BIT(7)
-#define QSGMII_PHY_DEEMPHASIS_LVL_MASK		GENMASK(11, 10)
-#define QSGMII_PHY_DEEMPHASIS_LVL(x)		FIELD_PREP(QSGMII_PHY_DEEMPHASIS_LVL_MASK, (x))
-#define QSGMII_PHY_PHASE_LOOP_GAIN_MASK		GENMASK(14, 12)
-#define QSGMII_PHY_PHASE_LOOP_GAIN(x)		FIELD_PREP(QSGMII_PHY_PHASE_LOOP_GAIN_MASK, (x))
+#define QSGMII_PHY_DEEMPHASIS_LVL_MASK	GENMASK(11, 10)
+#define QSGMII_PHY_DEEMPHASIS_LVL(x)	FIELD_PREP(QSGMII_PHY_DEEMPHASIS_LVL_MASK, (x))
+#define QSGMII_PHY_PHASE_LOOP_GAIN_MASK	GENMASK(14, 12)
+#define QSGMII_PHY_PHASE_LOOP_GAIN(x)	FIELD_PREP(QSGMII_PHY_PHASE_LOOP_GAIN_MASK, (x))
 #define QSGMII_PHY_RX_DC_BIAS_MASK		GENMASK(19, 18)
 #define QSGMII_PHY_RX_DC_BIAS(x)		FIELD_PREP(QSGMII_PHY_RX_DC_BIAS_MASK, (x))
-#define QSGMII_PHY_RX_INPUT_EQU_MASK		GENMASK(21, 20)
+#define QSGMII_PHY_RX_INPUT_EQU_MASK	GENMASK(21, 20)
 #define QSGMII_PHY_RX_INPUT_EQU(x)		FIELD_PREP(QSGMII_PHY_RX_INPUT_EQU_MASK, (x))
 #define QSGMII_PHY_CDR_PI_SLEW_MASK		GENMASK(23, 22)
 #define QSGMII_PHY_CDR_PI_SLEW(x)		FIELD_PREP(QSGMII_PHY_CDR_PI_SLEW_MASK, (x))
@@ -103,6 +123,40 @@
 #define QSGMII_PHY_TX_DRV_AMP_MASK		GENMASK(31, 28)
 #define QSGMII_PHY_TX_DRV_AMP(x)		FIELD_PREP(QSGMII_PHY_TX_DRV_AMP_MASK, (x))
 
+#define MDIO_ADDR_NONE 0x20
+
+/* NSS Common registers (via nss_common regmap) */
+#define NSS_MACSEC_CTL                  0x28
+#define MACSEC_BYPASS_EXT_EN            BIT(7)
+#define NSS_QSGMII_CLK_CTL              0x2C
+
+/* QSGMII CSR registers */
+#define PCS_QSGMII_CTL                  0x20
+#define PCS_QSGMII_ATHR_CSCO_AUTONEG    BIT(0)
+#define PCS_QSGMII_SW_VER_1_7           BIT(1)
+#define PCS_QSGMII_CUTTHROUGH_TX        BIT(2)
+#define PCS_QSGMII_CUTTHROUGH_RX        BIT(3)
+#define PCS_QSGMII_SHORT_THRESH         BIT(4)
+#define PCS_QSGMII_SHORT_LATENCY        BIT(5)
+#define PCS_QSGMII_DEPTH_THRESH(n)      ((n) << 8)
+
+#define PCS_CH0_SERDES_SN_DETECT        BIT(11)
+#define PCS_CHn_SERDES_SN_DETECT(n)     (PCS_CH0_SERDES_SN_DETECT << (n))
+
+#define PCS_CH0_SERDES_SN_DETECT_2      BIT(16)
+#define PCS_CHn_SERDES_SN_DETECT_2(n)   (PCS_CH0_SERDES_SN_DETECT_2 << (n))
+
+#define QSGMII_PHY_MODE_CTL				0x64
+#define QSGMII_PHY_MODE_QSGMII			0x1
+
+#define PCS_QSGMII_SGMII_MODE			0x84
+#define PCS_QSGMII_MODE_QSGMII			0x0
+
+#define QSGMII_PHY_QSGMII_CTL			0x7C
+#define QSGMII_TX_SLC_CTL(n)			BIT(28 + (n))
+
+static atomic_t gmac_probe_order = ATOMIC_INIT(0);
+
 struct ipq806x_gmac {
 	struct platform_device *pdev;
 	struct regmap *nss_common;
@@ -110,6 +164,7 @@ struct ipq806x_gmac {
 	uint32_t id;
 	struct clk *core_clk;
 	phy_interface_t phy_mode;
+	struct stmmac_resources stmmac_res;
 };
 
 static int get_clk_div_sgmii(struct ipq806x_gmac *gmac, unsigned int speed)
@@ -180,6 +235,7 @@ static int ipq806x_gmac_set_speed(struct
 		break;
 
 	case PHY_INTERFACE_MODE_SGMII:
+	case PHY_INTERFACE_MODE_QSGMII:
 		div = get_clk_div_sgmii(gmac, speed);
 		clk_bits = NSS_COMMON_CLK_GATE_GMII_RX_EN(gmac->id) |
 			   NSS_COMMON_CLK_GATE_GMII_TX_EN(gmac->id);
@@ -207,6 +263,35 @@ static int ipq806x_gmac_set_speed(struct
 	regmap_read(gmac->nss_common, NSS_COMMON_CLK_GATE, &val);
 	val |= clk_bits;
 	regmap_write(gmac->nss_common, NSS_COMMON_CLK_GATE, val);
+	return 0;
+}
+
+static int ipq806x_gmac_configure_qsgmii_global(struct ipq806x_gmac *gmac)
+{
+	u32 pcs_qsgmii_ctl_val;
+
+	if (gmac->phy_mode != PHY_INTERFACE_MODE_QSGMII)
+		return 0;
+
+	/* Only once for first gmac */
+	if (gmac->id != 0)
+		return 0;
+
+	pcs_qsgmii_ctl_val = PCS_QSGMII_ATHR_CSCO_AUTONEG |
+						 PCS_QSGMII_SW_VER_1_7 |
+						 PCS_QSGMII_SHORT_THRESH |
+						 PCS_QSGMII_SHORT_LATENCY |
+						 PCS_QSGMII_DEPTH_THRESH(1) |
+						 PCS_CHn_SERDES_SN_DETECT(0) |
+						 PCS_CHn_SERDES_SN_DETECT(1) |
+						 PCS_CHn_SERDES_SN_DETECT(2) |
+						 PCS_CHn_SERDES_SN_DETECT(3) |
+						 PCS_CHn_SERDES_SN_DETECT_2(0) |
+						 PCS_CHn_SERDES_SN_DETECT_2(1) |
+						 PCS_CHn_SERDES_SN_DETECT_2(2) |
+						 PCS_CHn_SERDES_SN_DETECT_2(3);
+
+	regmap_write(gmac->qsgmii_csr, PCS_QSGMII_CTL, pcs_qsgmii_ctl_val);
 
 	return 0;
 }
@@ -214,6 +299,7 @@ static int ipq806x_gmac_set_speed(struct
 static int ipq806x_gmac_of_parse(struct ipq806x_gmac *gmac)
 {
 	struct device *dev = &gmac->pdev->dev;
+
 	int ret;
 
 	ret = of_get_phy_mode(dev->of_node, &gmac->phy_mode);
@@ -330,31 +416,32 @@ ipq806x_gmac_configure_qsgmii_params(str
 {
 	struct platform_device *pdev = gmac->pdev;
 	const struct soc_device_attribute *soc;
-	struct device *dev = &pdev->dev;
 	u32 qsgmii_param;
 
 	switch (gmac->id) {
+	case 0:
 	case 1:
 		soc = soc_device_match(ipq806x_gmac_soc_v1);
 
 		if (soc)
 			qsgmii_param = QSGMII_PHY_TX_DRV_AMP(0xc) |
-				       QSGMII_PHY_TX_SLEW(0x2) |
-				       QSGMII_PHY_DEEMPHASIS_LVL(0x2);
+					   QSGMII_PHY_TX_SLEW(0x2) |
+					   QSGMII_PHY_DEEMPHASIS_LVL(0x2);
 		else
 			qsgmii_param = QSGMII_PHY_TX_DRV_AMP(0xd) |
-				       QSGMII_PHY_TX_SLEW(0x0) |
-				       QSGMII_PHY_DEEMPHASIS_LVL(0x0);
+					   QSGMII_PHY_TX_SLEW(0x0) |
+					   QSGMII_PHY_DEEMPHASIS_LVL(0x0);
 
 		qsgmii_param |= QSGMII_PHY_RX_DC_BIAS(0x2);
 		break;
 	case 2:
 	case 3:
 		qsgmii_param = QSGMII_PHY_RX_DC_BIAS(0x3) |
-			       QSGMII_PHY_TX_DRV_AMP(0xc);
+				   QSGMII_PHY_TX_DRV_AMP(0xc);
 		break;
-	default: /* gmac 0 can't be set in SGMII mode */
-		dev_err(dev, "gmac id %d can't be in SGMII mode", gmac->id);
+	default:
+		dev_err(&pdev->dev, "invalid gmac id %d for SGMII/QSGMII mode\n",
+			gmac->id);
 		return -EINVAL;
 	}
 
@@ -369,7 +456,7 @@ ipq806x_gmac_configure_qsgmii_params(str
 			QSGMII_PHY_CDR_PI_SLEW(0x2);
 
 	regmap_write(gmac->qsgmii_csr, QSGMII_PHY_SGMII_CTL(gmac->id),
-		     qsgmii_param);
+			 qsgmii_param);
 
 	return 0;
 }
@@ -377,38 +464,38 @@ ipq806x_gmac_configure_qsgmii_params(str
 static int ipq806x_gmac_probe(struct platform_device *pdev)
 {
 	struct plat_stmmacenet_data *plat_dat;
-	struct stmmac_resources stmmac_res;
 	struct device *dev = &pdev->dev;
 	struct ipq806x_gmac *gmac;
-	int val;
-	int err;
-
-	val = stmmac_get_platform_resources(pdev, &stmmac_res);
-	if (val)
-		return val;
-
-	plat_dat = devm_stmmac_probe_config_dt(pdev, stmmac_res.mac);
-	if (IS_ERR(plat_dat))
-		return PTR_ERR(plat_dat);
+	int val, err;
 
+	/* Parse device tree to get GMAC ID - minimal parsing only */
 	gmac = devm_kzalloc(dev, sizeof(*gmac), GFP_KERNEL);
 	if (!gmac)
 		return -ENOMEM;
 
 	gmac->pdev = pdev;
-
 	err = ipq806x_gmac_of_parse(gmac);
 	if (err) {
 		dev_err(dev, "device tree parsing error\n");
 		return err;
 	}
 
-	regmap_write(gmac->qsgmii_csr, QSGMII_PCS_CAL_LCKDT_CTL,
-		     QSGMII_PCS_CAL_LCKDT_CTL_RST);
+	/* qsgmii enforce probe order */
+	if (gmac->phy_mode == PHY_INTERFACE_MODE_QSGMII &&
+				atomic_read(&gmac_probe_order) != gmac->id)
+			return -EPROBE_DEFER;
+
+	val = stmmac_get_platform_resources(pdev, &gmac->stmmac_res);
+	if (val)
+		return val;
+
+	plat_dat = devm_stmmac_probe_config_dt(pdev, gmac->stmmac_res.mac);
+	if (IS_ERR(plat_dat))
+		return PTR_ERR(plat_dat);
 
 	/* Inter frame gap is set to 12 */
 	val = 12 << NSS_COMMON_GMAC_CTL_IFG_OFFSET |
-	      12 << NSS_COMMON_GMAC_CTL_IFG_LIMIT_OFFSET;
+		  12 << NSS_COMMON_GMAC_CTL_IFG_LIMIT_OFFSET;
 	/* We also initiate an AXI low power exit request */
 	val |= NSS_COMMON_GMAC_CTL_CSYS_REQ;
 	switch (gmac->phy_mode) {
@@ -419,6 +506,7 @@ static int ipq806x_gmac_probe(struct pla
 		val |= NSS_COMMON_GMAC_CTL_PHY_IFACE_SEL;
 		break;
 	case PHY_INTERFACE_MODE_SGMII:
+	case PHY_INTERFACE_MODE_QSGMII:
 		val &= ~NSS_COMMON_GMAC_CTL_PHY_IFACE_SEL;
 		break;
 	default:
@@ -441,11 +529,20 @@ static int ipq806x_gmac_probe(struct pla
 		val |= NSS_COMMON_CLK_SRC_CTRL_SGMII(gmac->id) <<
 			NSS_COMMON_CLK_SRC_CTRL_OFFSET(gmac->id);
 		break;
+	case PHY_INTERFACE_MODE_QSGMII:
+		val |= NSS_COMMON_CLK_SRC_CTRL_QSGMII(gmac->id) <<
+			NSS_COMMON_CLK_SRC_CTRL_OFFSET(gmac->id);
+		break;
 	default:
 		goto err_unsupported_phy;
 	}
 	regmap_write(gmac->nss_common, NSS_COMMON_CLK_SRC_CTRL, val);
 
+	/* For QSGMII, clear clock halt bits */
+	if (gmac->phy_mode == PHY_INTERFACE_MODE_QSGMII) {
+		regmap_write(gmac->nss_common, NSS_QSGMII_CLK_CTL, 0);
+	}
+
 	/* Enable PTP clock */
 	regmap_read(gmac->nss_common, NSS_COMMON_CLK_GATE, &val);
 	val |= NSS_COMMON_CLK_GATE_PTP_EN(gmac->id);
@@ -458,6 +555,7 @@ static int ipq806x_gmac_probe(struct pla
 			NSS_COMMON_CLK_GATE_RGMII_TX_EN(gmac->id);
 		break;
 	case PHY_INTERFACE_MODE_SGMII:
+	case PHY_INTERFACE_MODE_QSGMII:
 		val |= NSS_COMMON_CLK_GATE_GMII_RX_EN(gmac->id) |
 				NSS_COMMON_CLK_GATE_GMII_TX_EN(gmac->id);
 		break;
@@ -466,24 +564,50 @@ static int ipq806x_gmac_probe(struct pla
 	}
 	regmap_write(gmac->nss_common, NSS_COMMON_CLK_GATE, val);
 
-	if (gmac->phy_mode == PHY_INTERFACE_MODE_SGMII) {
-		err = ipq806x_gmac_configure_qsgmii_params(gmac);
-		if (err)
+	switch (gmac->phy_mode) {
+	case PHY_INTERFACE_MODE_SGMII:
+	case PHY_INTERFACE_MODE_QSGMII:
+		if (gmac->phy_mode != PHY_INTERFACE_MODE_QSGMII &&
+				((err = ipq806x_gmac_configure_qsgmii_params(gmac))))
 			return err;
 
-		err = ipq806x_gmac_configure_qsgmii_pcs_speed(gmac);
-		if (err)
+		if ((err = ipq806x_gmac_configure_qsgmii_pcs_speed(gmac)))
 			return err;
+
+		/* Global QSGMII config (GMAC3 only) */
+		if ((err = ipq806x_gmac_configure_qsgmii_global(gmac)))
+			return err;
+		break;
+	default:
+		break;
 	}
 
 	plat_dat->has_gmac = true;
 	plat_dat->bsp_priv = gmac;
-	plat_dat->fix_mac_speed = ipq806x_gmac_fix_mac_speed;
+	if (!of_phy_is_fixed_link(pdev->dev.of_node)) {
+		plat_dat->fix_mac_speed = ipq806x_gmac_fix_mac_speed;
+	}
 	plat_dat->multicast_filter_bins = 0;
 	plat_dat->tx_fifo_size = 8192;
 	plat_dat->rx_fifo_size = 8192;
 
-	return stmmac_dvr_probe(&pdev->dev, plat_dat, &stmmac_res);
+	err = stmmac_dvr_probe(&pdev->dev, plat_dat, &gmac->stmmac_res);
+	if (err)
+		return err;
+
+	if (of_property_read_bool(pdev->dev.of_node, "no-bridge")) {
+		struct net_device *ndev = platform_get_drvdata(pdev);
+		if(ndev){
+			ndev->priv_flags |= IFF_DONT_BRIDGE;
+		}
+	}
+
+	/* Increment for next GMAC */
+	if (gmac->phy_mode == PHY_INTERFACE_MODE_QSGMII) {
+		atomic_inc(&gmac_probe_order);
+	}
+
+	return 0;
 
 err_unsupported_phy:
 	dev_err(&pdev->dev, "Unsupported PHY mode: \"%s\"\n",
